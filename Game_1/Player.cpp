#include "Player.h"
#include "global.h"

Player::Player()
{
}

void Player::Render()
{
	/*TextureElement* element = textureManager.GetTexture(PLAYER);
	element->g_Sprite->Begin(D3DXSPRITE_ALPHABLEND);

	RECT srcRect;
	srcRect.left =0;
	srcRect.top = 0;
	srcRect.right = ;
	srcRect.bottom = 250;

	D3DXVECTOR3 pos(0, 200, 0);

	element->g_Sprite->Draw(element->g_pTexture, &srcRect, nullptr, &pos, D3DCOLOR_XRGB(255, 255, 255));

	element->g_Sprite->End();*/

}

void Player::Update()
{

}

